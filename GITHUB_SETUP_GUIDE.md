# GitHub 仓库设置指南

## 设置仓库简介

### 方法 1: 通过网页界面 (推荐)

1. **访问仓库主页**
   - 打开 https://github.com/jabing/memsearch-ts

2. **找到 About 区域**
   - 在页面**右侧边栏**
   - 通常在 "Releases" 或 "Packages" 上方
   - 会显示 "About" 标题

3. **点击编辑按钮**
   - 在 "About" 文字右侧有一个 ⚙️ **齿轮图标**
   - 点击它打开编辑窗口

4. **填写简介信息**

   **Description (描述):**
   ```
   Semantic memory search for markdown knowledge bases — TypeScript/Node.js port of memsearch built on Milvus vector database. Supports OpenAI, Google, Ollama, Voyage embeddings with Claude Code plugin integration.
   ```

   **Website (网站，可选):**
   ```
   https://www.npmjs.com/package/memsearch-core
   ```

5. **保存**
   - 点击 "Save changes" 按钮

---

### 方法 2: 使用 GitHub CLI

```bash
# 安装 gh CLI (如果未安装)
# Windows: winget install GitHub.cli

# 设置简介
gh api -X PATCH /repos/jabing/memsearch-ts \
  -f description="Semantic memory search for markdown knowledge bases — TypeScript/Node.js port of memsearch built on Milvus vector database. Supports OpenAI, Google, Ollama, Voyage embeddings with Claude Code plugin integration."
```

---

### 方法 3: 使用 curl

```bash
curl -X PATCH \
  https://api.github.com/repos/jabing/memsearch-ts \
  -H "Authorization: token YOUR_GITHUB_TOKEN" \
  -H "Accept: application/vnd.github.v3+json" \
  -d '{"description":"Semantic memory search for markdown knowledge bases — TypeScript/Node.js port of memsearch built on Milvus vector database. Supports OpenAI, Google, Ollama, Voyage embeddings with Claude Code plugin integration."}'
```

---

## 添加 Topics

Topics 帮助其他人发现你的项目。

### 步骤：

1. 在仓库主页右侧边栏找到 "About" 区域
2. 点击 ⚙️ 齿轮图标
3. 在弹出的窗口底部找到 "Topics" 输入框
4. 添加以下 topics (用空格或逗号分隔):

```
typescript semantic-search vector-database milvus markdown memory rag embeddings nodejs claude-code
```

5. 点击 "Save changes"

---

## 截图说明

### About 区域位置

```
┌─────────────────────────────────────────────────────────┐
│  jabing / memsearch-ts    Public                       │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  [代码] [Issues] [Pull Requests] [Actions] ...         │
│                                                         │
├───────────────────────┬─────────────────────────────────┤
│                       │  ⭐ About                       │
│  文件列表...          │     Semantic memory search...  │
│                       │     ⚙️ 编辑                     │
│                       │                                 │
│                       │  🏷️ Topics                     │
│                       │     typescript · semantic-s... │
│                       │                                 │
│                       │  📦 Releases                   │
│                       │  👥 Contributors               │
└───────────────────────┴─────────────────────────────────┘
```

### 编辑窗口

```
┌────────────────────────────────────────┐
│  Edit details                          │
├────────────────────────────────────────┤
│  Description                           │
│  ┌──────────────────────────────────┐ │
│  │ Semantic memory search for...    │ │
│  │                                  │ │
│  └──────────────────────────────────┘ │
│                                        │
│  Website                               │
│  ┌──────────────────────────────────┐ │
│  │ https://npmjs.com/package/...    │ │
│  └──────────────────────────────────┘ │
│                                        │
│  Topics                                │
│  ┌──────────────────────────────────┐ │
│  │ typescript · semantic-search ·.. │ │
│  └──────────────────────────────────┘ │
│                                        │
│  [Cancel]  [Save changes]              │
└────────────────────────────────────────┘
```

---

## 常见问题

### Q: 看不到 About 区域？

**A:** 尝试以下方法：
1. 刷新页面 (Ctrl+F5 或 Cmd+Shift+R)
2. 确保已登录 GitHub
3. 确保你是仓库所有者或有写入权限
4. 尝试切换到桌面版网站 (非移动版)

### Q: 没有编辑按钮？

**A:** 你需要有仓库的**写入权限**才能编辑简介。
- 如果你是仓库所有者，应该有权限
- 如果是他人的仓库，联系管理员添加你为协作者

### Q: Topics 无法保存？

**A:** 检查：
- Topics 不能超过 20 个
- 每个 topic 不能超过 35 个字符
- 只能使用小写字母、数字和连字符

---

## 完成后的效果

设置完成后，你的仓库主页会显示：

```
┌───────────────────────────────────────────────────────┐
│  ⭐ About                                            │
│     Semantic memory search for markdown knowledge... │
│     ⚙️ 编辑                                          │
│                                                       │
│  🏷️ Topics                                           │
│     typescript · semantic-search · vector-database   │
│     milvus · markdown · memory · rag · embeddings    │
│     nodejs · claude-code                             │
│                                                       │
│  📦 Releases                                         │
│     v1.0.0 Latest                                    │
│     2 days ago                                       │
└───────────────────────────────────────────────────────┘
```

---

## 下一步

设置完简介后：

1. ✅ 推送到 GitHub
   ```bash
   git push -u origin main
   git push origin v1.0.0
   ```

2. ✅ 发布到 npm
   ```bash
   export NPM_TOKEN="npm_xxx"
   pnpm publish --filter memsearch-core --access public
   pnpm publish --filter memsearch-cli --access public
   ```

3. ✅ 创建 GitHub Release
   - 访问 https://github.com/jabing/memsearch-ts/releases/new
   - 选择 Tag v1.0.0
   - 点击 "Generate release notes"
   - 点击 "Publish release"

---

**完成！** 🎉
